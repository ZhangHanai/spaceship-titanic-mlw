# Reference submissions

Place the public reference submission used by the final competition-side pipeline here:

```text
submissions/reference/public_reference_submission.csv
```

The file is not committed by default. It must contain exactly two columns:
`PassengerId` and `Transported`.
